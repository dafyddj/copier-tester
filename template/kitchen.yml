---
driver:
  name: docker

provisioner:
  name: dummy

verifier:
  name: shell

platforms:
  - name: test
    driver:
      image: dafyddj/base-tester:local-test
      platform: debian

suites:
  - name: default
